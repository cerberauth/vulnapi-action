name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-typescript:
    name: TypeScript Tests
    uses: cerberauth/ci/.github/workflows/action-ci.yml@main

  test-failed-scans:
    name: GitHub Actions Test Failed Scans
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read

    env:
      DOCKER_IMAGE: ghcr.io/cerberauth/api-vulns-challenges/jwt-alg-none-bypass:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get JWT
        id: get-jwt
        run: |
          echo "jwt=$(docker run --rm ${{ env.DOCKER_IMAGE }} jwt)" >> "$GITHUB_OUTPUT"

      - name: Run Server
        run: |
          docker run -d -p 8080:8080 ${{ env.DOCKER_IMAGE }}
          sleep 5
          curl --verbose http://localhost:8080 -H "Authorization: Bearer ${{ steps.get-jwt.outputs.jwt }}"

      - name: Test cURL Local Action
        id: test-curl-action
        continue-on-error: true
        uses: ./
        with:
          scans: jwt.*
          curl: |
            curl http://localhost:8080 -H "Authorization: Bearer ${{ steps.get-jwt.outputs.jwt }}"

      - name: Check scan detected vulnerabilities
        if: ${{ steps.test-curl-action.outcome != 'failure' }}
        run: |
          echo "Expected scan to fail but it succeeded (no vulnerabilities detected)"
          exit 1

      # - name: Test OpenAPI Local Action
      #   uses: ./
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     scans: jwt.*
      #     openapi: ./__tests__/openapi.yaml

      - name: Stop Server
        if: ${{ always() }}
        run:
          docker stop "$(docker ps -q --filter ancestor=${{ env.DOCKER_IMAGE
          }})"

  test-scans:
    name: GitHub Actions Test Scans
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read

    env:
      DOCKER_IMAGE: ghcr.io/cerberauth/api-vulns-challenges/jwt-strong-eddsa-key:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get JWT
        id: get-jwt
        run: |
          echo "jwt=$(docker run --rm ${{ env.DOCKER_IMAGE }} jwt)" >> "$GITHUB_OUTPUT"

      - name: Run Server
        run: |
          docker run -d -p 8080:8080 ${{ env.DOCKER_IMAGE }}
          sleep 5
          curl --verbose http://localhost:8080 -H "Authorization: Bearer ${{ steps.get-jwt.outputs.jwt }}"

      - name: Test cURL Local Action with rate limit and excluded scans
        uses: ./
        with:
          rateLimit: 1000/s
          excludeScans: misconfiguration.*
          curl: |
            curl http://localhost:8080 -H "Authorization: Bearer ${{ steps.get-jwt.outputs.jwt }}"

      - name: Test cURL Local Action with selected scans
        uses: ./
        with:
          scans: jwt.*
          curl: |
            curl http://localhost:8080 -H "Authorization: Bearer ${{ steps.get-jwt.outputs.jwt }}"

      - name: Test cURL Local Action without Telemetry
        uses: ./
        with:
          scans: jwt.*
          curl: |
            curl http://localhost:8080 -H "Authorization: Bearer ${{ steps.get-jwt.outputs.jwt }}"
          telemetry: false

      # - name: Test OpenAPI Local Action
      #   uses: ./
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     scans: jwt.*
      #     openapi: ./__tests__/openapi.yaml
      #     telemetry: false

      - name: Stop Server
        if: ${{ always() }}
        run:
          docker stop "$(docker ps -q --filter ancestor=${{ env.DOCKER_IMAGE
          }})"
